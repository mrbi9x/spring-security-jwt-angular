server:
    port: 8080

spring:
    datasource:
        url: jdbc:h2:file:./h2/auth-service-h2
        driverClassName: org.h2.Driver
        username: root
        password: toor
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: update
    h2:
        console:
            enabled: true
            path: /h2-console
            settings:
                web-allow-others: true

security:
    jwt:
        uri: /auth/**
        header: Authorization
        prefix: Bearer 
        expiration: 300
        secret: JwtS3cr3tK3y